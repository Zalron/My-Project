using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ColonyView : MonoBehaviour
{
    //Get called every time we become active (including possibly on scene load)
    private void OnEnable()
    {
        //Update our data elements based on the selected colony
    }

    public object Colony;

    // Start is called before the first frame update. Use this for one-time initialization, AFTER OnEnable is called
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
